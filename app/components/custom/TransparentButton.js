var React = require('react');
var ReactRouter = require('react-router');

var TransparentButton = React.createClass({
// having form with className form two works well, but not in design
render: function(){
return (
  <div> </div>
);
}
});

module.exports = TransparentButton;
